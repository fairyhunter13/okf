Reference bundles copied verbatim from `GoogleCloudPlatform/knowledge-catalog`
(`okf/bundles/`, Apache 2.0), minus the generated `viz.html` files.

They pin conformance to something outside our own reading of the spec: whatever
`okf check` rejects here is a bug in the checker, not in the bundle.
